
# Kraken Fee Inspector (MVP)

## Local run
````bash
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
uvicorn app.main:app --reload
````

Then open http://127.0.0.1:8000 in your browser.

## Render deploy
1. Create a *new Web Service* from this repo.
2. Select **Free plan**, Python environment.
3. Ensure `render.yaml` is detected.
4. Deploy – Render builds & gives you a URL like  
   `https://fee-inspector.onrender.com`.

## Next steps
* Replace dummy fee calculation with real Kraken API calls
* Add Stripe paywall
* Add Telegram bot endpoint
